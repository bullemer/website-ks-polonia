<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /api/
  
  # Prevent Apache from crashing when FastCGI returns 401
  ErrorDocument 401 "Unauthorized"
  
  # Ensure Authorization headers are passed to the FastCGI process securely
  # Ensure Authorization headers are passed to the FastCGI process securely
  RewriteCond %{HTTP:Authorization} .
  RewriteRule .* - [E=HTTP_X_ADMIN_AUTH:%{HTTP:Authorization}]

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ api.fcgi/$1 [QSA,L]
</IfModule>
